---
description: YOLOv11 加油站工人未穿戴工服检测项目的全局编码规则
globs: []
alwaysApply: true
---

你正在 `E:\University_competition\Innovation_Entrepreneurship\MyProgram\yolo-program` 项目中工作。

# 核心目标

所有代码生成、代码修改、重构、补全、设计建议，都必须严格服务于以下目标：

`YOLOv11` 加油站工人未穿戴工服检测。

不得默认沿用旧警务场景语义，不得把历史上的 police、police uniform、警服、派出所 等概念继续扩散到新实现中，除非当前任务明确要求兼容旧逻辑。

# 编码前强制文档检查

在生成代码前，必须先结合 `docs` 目录中的文档进行判断，并将文档作为最高优先级标准。

至少优先参考以下文件：

1. `docs/yolov11_workwear_system_design.md`
2. `docs/yolov11_migration_reuse_rewrite.md`
3. `docs/code_change_log.md`

如果任务范围较小，可以优先读取最相关章节，但不能跳过文档检查。

# 强制实现规则

每次编码都必须满足以下要求：

1. 以文档为规范，以代码为落地实现，文档优先级高于旧代码习惯。
2. 所有检测语义都必须围绕 `YOLOv11`、`gas station worker`、`workwear missing` 展开。
3. 生成的代码必须逻辑严谨、可落地、可推演，不能只停留在表面重命名或演示性伪实现。
4. 代码风格必须统一，命名要稳定，尤其是 `workwear`、`worker`、`camera`、`violation`、`rule_code`、`rule_name`、`station_id`、`dept_id`、`sub_id`。
5. 不允许凭空创造文档没有支持的业务规则。
6. 当旧代码与文档冲突时，以文档定义的新目标行为为准，仅保留必要兼容层。

# 输出风格要求

1. 表达直接、专业、克制。
2. 严禁添加谄媚、夸张、情绪化或无信息增量的措辞。
3. 注释只解释真正不明显的逻辑，不写低价值注释。
4. 解释实现方案时，优先说明判断依据、边界条件和可落地性。

# 代码生成优先级

实现时按以下优先级思考：

1. 先保证业务语义正确。
2. 再保证 `person detection`、`workwear detection`、`temporal judgment`、`evidence output` 链路完整。
3. 再保证数据字段语义一致。
4. 最后再处理表现层和次要格式问题。

# 特殊约束

生成代码时，不需要优先在意 import、包解析、模块 wiring、环境拼装等问题。不要让这些问题阻塞核心业务逻辑设计，但必须保证主流程、接口语义、判断逻辑和数据结构本身是自洽的，后续可以统一补齐集成问题。

# 交付前自检

在结束输出前，确认：

1. 实现是否仍然严格服务于 `YOLOv11` 加油站工人未穿戴工服检测。
2. 是否无意间重新引入旧警务场景的规则、标签或命名。
3. 规则判断链路是否可以真正落地，而不是停留在概念层。
4. 字段语义是否与当前项目迁移方向一致。
